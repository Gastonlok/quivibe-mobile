import { Heading, Screen, ServiceState } from '../../components/ui';
export default function Detail() {
  return <Screen><Heading title="La communauté en parle" /><ServiceState message="Les avis de ce lieu ne sont pas encore accessibles dans l’application." icon="chatbubbles-outline" /></Screen>;
}
