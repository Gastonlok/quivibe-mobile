import { Heading, Screen, ServiceState } from '../../components/ui';
export default function Detail() {
  return <Screen><Heading title="Une sortie à partager" /><ServiceState message="Le détail de cet événement n’est pas encore accessible dans l’application." icon="calendar-outline" /></Screen>;
}
